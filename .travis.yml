language: python

os:
  - linux
  - osx

python:
  - "3.3"
  - "3.6"
  
cache: pip

before_install:
  - python --version
  - pip install -U pip
  
install:
  - pip install -r requirements.txt

script:
  - python DigitalEpidemiology.py
